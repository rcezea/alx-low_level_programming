#include "main.h"
/**
 * add - adds two integers and returns the result
 * @x: first integer
 * @y: secong integer
 * Return: Always true
 */
int add(int x, int y)
{
	int sum = x + y;

	return (sum);
}
