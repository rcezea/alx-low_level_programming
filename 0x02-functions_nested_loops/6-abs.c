#include "main.h"
/**
 * _abs - computes absolute value of an integer
 * @n: integer to be checked
 * Return: ALways
 */
int _abs(int n)
{
	if (n >= 0)
	{
		n = n;
	}
	else
	{
		n = -n;
	}
	return (n);
}
